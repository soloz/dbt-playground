version: 2

models:
  - name: stg_orders
    description: one unique order per row
    columns:
      - name: orderid
        description: primary key for staging orders
      - name: shipmode
        description: '{{doc("shipmode")}}'